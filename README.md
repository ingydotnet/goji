goji
====

goji samples

# Makefile

* `make build`
* `make install`
* `make get`
* `make clean`

# goji json sample

Run server:

```
$ go run json.go
```

Request to the server:

```
curl http://localhost:8000/hello/gopher
```
